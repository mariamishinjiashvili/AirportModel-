# AirportModel-
AirportModel is a Java-based airport database management application that provides a graphical interface for creating, viewing, updating, and deleting passenger and check-in location records. The application uses Java Swing for the GUI and SQL/Flyway for database management.
